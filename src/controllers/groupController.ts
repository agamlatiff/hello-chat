import type { NextFunction, Request, Response } from "express";

export const createFreeGroup = async (
  req: Request,
  res: Response,
  next: NextFunction
) => {
  try {
    
  } catch (error) {
    next(error)
  }
}